# Manyfold publishes semver tags without the leading "v" from GitHub releases
# (release v0.146.0 → image manyfold3d/manyfold:0.146.0). Keep aligned with
# docs/railway-wiring.md. Weekly upstream-check compares GitHub tag_name
# (with "v"); review bump PRs and strip the "v" if the rewritten FROM does
# not resolve on Docker Hub.
FROM manyfold3d/manyfold:0.146.0
